name: Build and Upload Binary

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # Checkout the code
      - name: Checkout code
        uses: actions/checkout@v4

      # Install dependencies (replace these with your actual libraries)
      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libgtk-4-dev \
            libcairo2-dev \
            libglib2.0-dev \
            libcurl4-openssl-dev \
            build-essential \
            cmake \
            pkg-config

      # Create build directory and build the project
      - name: Build project
        run: |
          chmod +x ./build.sh &&
          ./build.sh

      # Upload binary as artifact
      - name: Upload binary
        uses: actions/upload-artifact@v4
        with:
          name: ElysiaDownloader
          path: |
            build/ElysiaDownloader
